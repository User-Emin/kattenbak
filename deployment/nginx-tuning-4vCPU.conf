# Nginx tuning voor 4vCPU / 16GB – duizenden concurrente gebruikers
# Voeg toe aan /etc/nginx/nginx.conf (in het hoofdgedeelte) of include in http-block
#
# Gebruik: merge de onderstaande regels in je bestaande nginx.conf

# Worker tuning (in main context, buiten http)
worker_processes 4;        # = aantal vCPU
worker_rlimit_nofile 8192; # max open files per worker

# In http-block:
# worker_connections 4096;  # per worker
# multi_accept on;
# use epoll;  # Linux only (default op moderne Linux)
